---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<div class="flex flex-wrap gap-1.5">
  {tags.map((tag) => (
    <span class="inline-block bg-stone-200 text-stone-600 text-xs px-2 py-0.5 rounded-full">
      {tag}
    </span>
  ))}
</div>
